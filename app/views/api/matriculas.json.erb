[
<% @matriculas.each_with_index do |matricula,index| %>
    {
      "id": <%= matricula.id %>,
      "bastidor": "<%= matricula.bastidor %>",
      "matricula": "<%= matricula.matricula %>",
      "fecha_matriculacion": "<%= l matricula.pdf_updated_at, format: '%Y-%m-%d' %>",
      "ivtm": "<%= matricula.ivtm %>"
    }
	<% unless index == @matriculas.count - 1 %>
    ,
    <% end %>
<% end %>
]